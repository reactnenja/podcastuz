import React from "react";
import Hero from "./hero";

const Header = () => {
    return (
        <header className="bg-[#F7EDE8] w-full h-auto">
            <Hero />
        </header>
    );
};

export default Header;
